import React from 'react';
import WindowManagementComponent from '../../component/Window-Management';

const WindowManagement = (props) => <WindowManagementComponent {...props} />;

export default WindowManagement;
